FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy agent module and server
COPY wiki_agent/ ./wiki_agent/
COPY main.py .

# Cloud Run injects PORT
ENV PORT=8080
EXPOSE 8080

CMD ["python", "main.py"]
